spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test
    username: sa
    password:

server:
  servlet:
    session:
#      ? ??
#      timeout: 1800
#      ? ??
      timeout: 30m